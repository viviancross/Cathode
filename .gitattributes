# Normalize text to LF in the repo; check out platform-appropriate endings.
* text=auto eol=lf

# Windows-only scripts keep CRLF.
*.bat text eol=crlf
*.ps1 text eol=crlf

# Binary assets — never touch.
*.png binary
*.ico binary
*.ttf binary
*.otf binary
*.zip binary
