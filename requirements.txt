# mpv itself is provided by Flatpak (io.mpv.Mpv) and controlled over a JSON
# IPC socket — we do NOT link libmpv in-process, so python-mpv is not needed.
# Networking (playlist/EPG/logo fetches) uses the stdlib urllib, so there's no
# `requests` dependency.
Pillow>=10.0.0
numpy>=1.24.0
